const API_URL = import.meta.env.VITE_API_URL || '/api'
async function request(path, options = {}) { const response = await fetch(`${API_URL}${path}`, { credentials: 'include', headers: { 'Content-Type': 'application/json', ...(options.headers || {}) }, ...options }); if (response.status === 204) return null; const data = await response.json().catch(() => ({})); if (!response.ok) { const error = new Error(data.error || 'Unable to complete this request.'); error.status = response.status; throw error } return data }
export const authService = { login: (credentials) => request('/auth/login', { method: 'POST', body: JSON.stringify(credentials) }), register: (details) => request('/auth/register', { method: 'POST', body: JSON.stringify(details) }), verifyEmail: (token) => request(`/auth/verify-email?token=${encodeURIComponent(token)}`), resendVerification: (details) => request('/auth/resend-verification', { method: 'POST', body: JSON.stringify(details) }), forgotPassword: (details) => request('/auth/forgot-password', { method: 'POST', body: JSON.stringify(details) }), resetPassword: (details) => request('/auth/reset-password', { method: 'POST', body: JSON.stringify(details) }), me: () => request('/auth/me'), logout: () => request('/auth/logout', { method: 'POST' }) }
